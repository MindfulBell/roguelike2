const INITIAL_HERO_STATE = {
    level: 1,
    hp: 50,
    weapon: ['fist', 5],
    position: [30,20]
}

export default function (state = INITIAL_HERO_STATE, action) {
    switch (action.type) {
        
    }
    return state;
}